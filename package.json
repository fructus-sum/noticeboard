{
  "name": "noticeboard",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client/display",
    "client/admin"
  ],
  "scripts": {
    "dev": "concurrently -n server,display,admin -c cyan,green,yellow \"npm -w noticeboard-server run dev\" \"npm -w noticeboard-display run dev\" \"npm -w noticeboard-admin run dev\"",
    "build": "npm -w noticeboard-display run build && npm -w noticeboard-admin run build",
    "start": "node server/index.js",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
